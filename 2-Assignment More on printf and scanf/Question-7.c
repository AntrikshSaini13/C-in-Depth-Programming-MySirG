// Write a program to input an ASCII coe from the user and print its corresponding characters
#include<stdio.h>
int main()
{
    int num;
    printf("Enter the number for ASCII code : ");
    scanf("%d",&num);
    printf("Character is %c",num);
    return 0;
}