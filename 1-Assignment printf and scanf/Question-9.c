// Write a program to calculate of area of cirlcle / TAke input appropriate data from the user.
#include<stdio.h>
int main()
{
    int num1,num2,area;
    printf("Enter two number first : ");
    scanf("%d",&num1);
    printf("Enter two number second : ");
    scanf("%d",&num2);
    area=num1*num2;
    printf("Area of rectangle is = %d",area);
    return 0;
}