// #write a program to calculate of size of character constant
#include<stdio.h>
int main()
{
    int size;
    size = sizeof('a');
    printf("\"The size of  character constant is = %d\"",size);
    return 0;
}